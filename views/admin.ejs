<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Admin - <%= APP_NAME %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/netflix.css">
    <style>
        body { display: flex; min-height: 100vh; }
        :root { --surface: #181818; --border: #2a2a2a; }

        .sidebar { width: 260px; background: #0a0a0a; border-right: 1px solid var(--border); padding: 25px 18px; display: flex; flex-direction: column; gap: 4px; position: sticky; top: 0; height: 100vh; }
        .sidebar .nf-logo { margin-bottom: 28px; }
        .nav-item {
            padding: 12px 14px; color: #b3b3b3; cursor: pointer;
            border-radius: 4px; transition: 0.15s; display: flex; align-items: center; gap: 12px;
            font-size: 14px; text-decoration: none;
        }
        .nav-item:hover { background: #232323; color: white; }
        .nav-item.active { background: rgba(229,9,20,0.15); color: white; border-left: 3px solid var(--nf-red); }

        .main { flex: 1; padding: 36px 40px; overflow-y: auto; }
        .panel { display: none; }
        .panel.active { display: block; animation: fadeIn 0.3s ease; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(8px); } to { opacity: 1; transform: none; } }

        h2 { font-size: 1.8rem; font-weight: 500; margin: 0 0 24px; padding-bottom: 14px; border-bottom: 1px solid var(--border); }
        h3 { font-size: 1.1rem; font-weight: 600; margin: 0 0 14px; }

        .card { background: var(--surface); border: 1px solid var(--border); border-radius: 6px; padding: 24px; margin-bottom: 24px; }
        .grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
        .grid-3 { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 16px; }
        .grid-4 { display: grid; grid-template-columns: 1fr 1fr 1fr 1fr; gap: 16px; }
        .field { margin-bottom: 12px; }
        label { display: block; color: #b3b3b3; font-size: 12px; margin-bottom: 6px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.5px; }

        input[type="text"], input[type="password"], input[type="number"], input[type="color"], select, textarea {
            width: 100%; background: #0a0a0a; border: 1px solid var(--border);
            color: white; padding: 10px 12px; border-radius: 4px;
            font-size: 14px; font-family: inherit; box-sizing: border-box;
        }
        input:focus, select:focus, textarea:focus { outline: none; border-color: var(--nf-red); }
        textarea { min-height: 90px; resize: vertical; }

        .checkbox-row { display: flex; align-items: center; gap: 10px; padding: 12px 0; }
        .checkbox-row input[type="checkbox"] { width: 18px; height: 18px; accent-color: var(--nf-red); }
        .checkbox-row label { margin-bottom: 0; text-transform: none; font-size: 14px; color: #e5e5e5; font-weight: 500; }

        .upload-box {
            border: 2px dashed #444; padding: 18px; text-align: center;
            border-radius: 6px; cursor: pointer; transition: 0.2s;
            background: #0a0a0a; position: relative;
            font-size: 13px; color: #b3b3b3;
        }
        .upload-box i { font-size: 24px; display: block; margin-bottom: 6px; color: #888; }
        .upload-box:hover { border-color: var(--nf-red); background: #1a1a1a; }
        .upload-box input[type="file"] { position: absolute; inset: 0; opacity: 0; cursor: pointer; }
        .file-name { color: var(--nf-red); font-weight: 600; display: block; margin-top: 6px; font-size: 12px; word-break: break-all; }

        .nf-btn { background: var(--nf-red); color: white; border: none; padding: 11px 24px; border-radius: 4px; font-weight: 700; cursor: pointer; transition: 0.2s; display: inline-flex; align-items: center; gap: 8px; }
        .nf-btn:hover { filter: brightness(1.1); }
        .btn-secondary { background: #444; color: white; }
        .btn-secondary:hover { background: #555; }
        .btn-success { background: #46d369; color: black; }

        table { width: 100%; border-collapse: collapse; background: var(--surface); border-radius: 6px; overflow: hidden; }
        th, td { padding: 12px 14px; text-align: left; border-bottom: 1px solid var(--border); }
        th { background: #232323; color: #b3b3b3; font-size: 12px; text-transform: uppercase; letter-spacing: 1px; }
        td { font-size: 14px; }
        td img.row-thumb { width: 80px; height: 45px; border-radius: 3px; object-fit: cover; }
        td img.row-avatar { width: 32px; height: 32px; border-radius: 50%; object-fit: cover; margin-right: 10px; vertical-align: middle; }

        .badge { padding: 3px 8px; border-radius: 3px; font-size: 11px; font-weight: 700; text-transform: uppercase; }
        .badge-admin { background: var(--nf-red); color: white; }
        .badge-viewer { background: #2ecc71; color: black; }
        .badge-type { background: #333; color: #e5e5e5; }

        .action-btn { background: none; border: none; color: #888; cursor: pointer; padding: 6px 10px; font-size: 14px; transition: 0.15s; border-radius: 3px; }
        .action-btn:hover { color: white; background: #333; }
        .action-btn.danger:hover { color: #ff5b5b; }

        @media (max-width: 900px) {
            .grid-2, .grid-3, .grid-4 { grid-template-columns: 1fr; }
            .sidebar { display: none; }
            .main { padding: 20px; }
        }
    </style>
</head>
<body>

    <aside class="sidebar">
        <a href="/" class="nf-logo" style="text-decoration:none;"><%= APP_NAME %></a>
        <div class="nav-item active" onclick="switchTab('library', this)"><i class="fas fa-film"></i> Library</div>
        <div class="nav-item" onclick="switchTab('users', this)"><i class="fas fa-users"></i> Users</div>
        <div class="nav-item" onclick="switchTab('settings', this)"><i class="fas fa-cogs"></i> Settings</div>
        <div style="flex:1;"></div>
        <a href="/" class="nav-item"><i class="fas fa-arrow-left"></i> Back to Browse</a>
    </aside>

    <main class="main">
        <div id="library" class="panel active">
            <h2>Library</h2>

            <div class="card">
                <h3>Add Movie or Series</h3>
                <form action="/admin/media/add" method="POST" enctype="multipart/form-data">
                    <div class="grid-2">
                        <div class="field">
                            <label>Title</label>
                            <input type="text" name="title" required>
                        </div>
                        <div class="field">
                            <label>Type</label>
                            <select name="type">
                                <option value="movie">Movie</option>
                                <option value="series">Series</option>
                            </select>
                        </div>
                    </div>

                    <div class="field">
                        <label>Description</label>
                        <textarea name="description" placeholder="A brief synopsis…"></textarea>
                    </div>

                    <div class="grid-4">
                        <div class="field">
                            <label>Category / Row</label>
                            <input type="text" name="category" placeholder="Action, Drama…" required>
                        </div>
                        <div class="field">
                            <label>Year</label>
                            <input type="number" name="releaseYear" placeholder="2025">
                        </div>
                        <div class="field">
                            <label>Duration (min)</label>
                            <input type="number" name="duration" placeholder="120">
                        </div>
                        <div class="field">
                            <label>Maturity</label>
                            <input type="text" name="maturity" placeholder="TV-MA / PG-13">
                        </div>
                    </div>

                    <div class="field">
                        <label>Genres (comma-separated)</label>
                        <input type="text" name="genres" placeholder="Action, Thriller, Sci-Fi">
                    </div>

                    <div class="grid-4">
                        <div class="field">
                            <label>Cover (Poster 16:9)</label>
                            <div class="upload-box">
                                <i class="fas fa-image"></i>
                                Drop cover
                                <span class="file-name" id="thumbN"></span>
                                <input type="file" name="thumbnail" accept="image/*" onchange="showFn(this,'thumbN')">
                            </div>
                        </div>
                        <div class="field">
                            <label>Backdrop (Hero)</label>
                            <div class="upload-box">
                                <i class="fas fa-images"></i>
                                Drop backdrop
                                <span class="file-name" id="backN"></span>
                                <input type="file" name="backdrop" accept="image/*" onchange="showFn(this,'backN')">
                            </div>
                        </div>
                        <div class="field">
                            <label>Logo (PNG)</label>
                            <div class="upload-box">
                                <i class="fas fa-tag"></i>
                                Drop logo
                                <span class="file-name" id="logoN"></span>
                                <input type="file" name="logo" accept="image/*" onchange="showFn(this,'logoN')">
                            </div>
                        </div>
                        <div class="field">
                            <label>Video File</label>
                            <div class="upload-box">
                                <i class="fas fa-video"></i>
                                Drop video
                                <span class="file-name" id="vidN"></span>
                                <input type="file" name="video" accept="video/*" onchange="showFn(this,'vidN')">
                            </div>
                        </div>
                    </div>

                    <div class="checkbox-row">
                        <input type="checkbox" id="trending" name="trending">
                        <label for="trending">Show in Trending Now row</label>
                    </div>

                    <button class="nf-btn" type="submit"><i class="fas fa-plus"></i> Add Content</button>
                </form>
            </div>

            <% if (media.some(m => m.type === 'series')) { %>
                <div class="card">
                    <h3>Add Episode</h3>
                    <form action="/admin/episode/add" method="POST" enctype="multipart/form-data">
                        <div class="grid-2">
                            <div class="field">
                                <label>Series</label>
                                <select name="parentId" required>
                                    <% media.filter(m => m.type === 'series').forEach(s => { %>
                                        <option value="<%= s.id %>"><%= s.title %></option>
                                    <% }) %>
                                </select>
                            </div>
                            <div class="field">
                                <label>Episode #</label>
                                <input type="number" name="order" placeholder="1" required>
                            </div>
                        </div>

                        <div class="grid-2">
                            <div class="field">
                                <label>Episode Title</label>
                                <input type="text" name="title">
                            </div>
                            <div class="field">
                                <label>Duration (min)</label>
                                <input type="number" name="duration" placeholder="45">
                            </div>
                        </div>

                        <div class="field">
                            <label>Episode Description</label>
                            <textarea name="description"></textarea>
                        </div>

                        <div class="grid-2">
                            <div class="field">
                                <label>Episode Still</label>
                                <div class="upload-box">
                                    <i class="fas fa-image"></i>
                                    Drop image
                                    <span class="file-name" id="epThumbN"></span>
                                    <input type="file" name="thumbnail" accept="image/*" onchange="showFn(this,'epThumbN')">
                                </div>
                            </div>
                            <div class="field">
                                <label>Episode Video</label>
                                <div class="upload-box">
                                    <i class="fas fa-video"></i>
                                    Drop video
                                    <span class="file-name" id="epVidN"></span>
                                    <input type="file" name="video" accept="video/*" onchange="showFn(this,'epVidN')" required>
                                </div>
                            </div>
                        </div>

                        <button class="nf-btn" type="submit"><i class="fas fa-plus"></i> Add Episode</button>
                    </form>
                </div>
            <% } %>

            <div class="card">
                <h3>Content (<%= media.length %>)</h3>
                <table>
                    <thead>
                        <tr><th>Title</th><th>Type</th><th>Category</th><th>Year</th><th>Trending</th><th></th></tr>
                    </thead>
                    <tbody>
                        <% media.forEach(m => { %>
                            <tr>
                                <td>
                                    <% if (m.thumbnail) { %><img src="<%= m.thumbnail %>" class="row-thumb"><% } %>
                                    <%= m.title %>
                                </td>
                                <td><span class="badge badge-type"><%= m.type %></span></td>
                                <td><%= m.category %></td>
                                <td><%= m.releaseYear || '—' %></td>
                                <td><%= m.trending ? '⭐' : '—' %></td>
                                <td style="text-align:right;">
                                    <form action="/admin/media/delete/<%= m.id %>" method="POST" style="display:inline;" onsubmit="return confirm('Delete this and its episodes?')">
                                        <button class="action-btn danger" title="Delete"><i class="fas fa-trash"></i></button>
                                    </form>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>

        <div id="users" class="panel">
            <h2>Users</h2>
            <div class="card">
                <h3 id="userFormTitle">Create New User</h3>
                <form id="userForm" action="/admin/user/add" method="POST" enctype="multipart/form-data">
                    <input type="hidden" name="id" id="editUserId">
                    <div class="grid-3">
                        <div class="field">
                            <label>Username</label>
                            <input type="text" name="username" id="editUsername" required>
                        </div>
                        <div class="field">
                            <label>Password <span id="passHint" style="text-transform:none; font-size:11px; color:#888;"></span></label>
                            <input type="password" name="password" id="editPassword" placeholder="Enter password">
                        </div>
                        <div class="field">
                            <label>Role</label>
                            <select name="role" id="editRole">
                                <option value="viewer">Viewer</option>
                                <option value="admin">Admin</option>
                            </select>
                        </div>
                    </div>
                    <div class="field">
                        <label>Avatar</label>
                        <div class="upload-box">
                            <i class="fas fa-user-circle"></i>
                            Drop avatar
                            <span class="file-name" id="userAvN"></span>
                            <input type="file" name="avatar" accept="image/*" onchange="showFn(this,'userAvN')">
                        </div>
                    </div>
                    <button class="nf-btn" id="userSubmitBtn" type="submit"><i class="fas fa-plus"></i> Create User</button>
                    <button type="button" class="nf-btn btn-secondary" id="userCancelBtn" style="display:none;" onclick="resetUserForm()">Cancel</button>
                </form>
            </div>

            <div class="card">
                <h3>Users (<%= users.length %>)</h3>
                <table>
                    <thead><tr><th>User</th><th>Role</th><th>Provider</th><th></th></tr></thead>
                    <tbody>
                        <% users.forEach(u => { %>
                            <tr>
                                <td>
                                    <img src="<%= u.avatar || ('https://ui-avatars.com/api/?name=' + encodeURIComponent(u.username)) %>" class="row-avatar">
                                    <%= u.username %>
                                </td>
                                <td><span class="badge badge-<%= u.role %>"><%= u.role %></span></td>
                                <td><%= u.auth_provider %></td>
                                <td style="text-align:right;">
                                    <button class="action-btn" onclick='editUser(<%- JSON.stringify(u) %>)' title="Edit"><i class="fas fa-pencil-alt"></i></button>
                                    <% if (u.username !== 'admin') { %>
                                        <form action="/admin/user/delete/<%= u.id %>" method="POST" style="display:inline;" onsubmit="return confirm('Delete this user?')">
                                            <button class="action-btn danger" title="Delete"><i class="fas fa-trash"></i></button>
                                        </form>
                                    <% } %>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>

        <div id="settings" class="panel">
            <h2>Settings</h2>
            <form action="/admin/settings/update" method="POST">
                <div class="card">
                    <h3>Branding</h3>
                    <div class="grid-2">
                        <div class="field"><label>App Name</label><input type="text" name="APP_NAME" value="<%= config.APP_NAME || 'MEDIATER' %>"></div>
                        <div class="field"><label>Accent Color</label><input type="color" name="ACCENT_COLOR" value="<%= config.ACCENT_COLOR || '#E50914' %>" style="height:42px; padding:4px;"></div>
                    </div>
                </div>
                <div class="card">
                    <h3>Single Sign-On (OIDC / Authentik / Keycloak)</h3>
                    <div class="grid-2">
                        <div class="field"><label>Client ID</label><input type="text" name="OIDC_CLIENT_ID" value="<%= config.OIDC_CLIENT_ID || '' %>"></div>
                        <div class="field"><label>Client Secret</label><input type="password" name="OIDC_CLIENT_SECRET" value="<%= config.OIDC_CLIENT_SECRET || '' %>"></div>
                    </div>
                    <div class="field"><label>Issuer URL</label><input type="text" name="OIDC_ISSUER" value="<%= config.OIDC_ISSUER || '' %>"></div>
                    <div class="grid-2">
                        <div class="field"><label>Auth URL</label><input type="text" name="OIDC_AUTH_URL" value="<%= config.OIDC_AUTH_URL || '' %>"></div>
                        <div class="field"><label>Token URL</label><input type="text" name="OIDC_TOKEN_URL" value="<%= config.OIDC_TOKEN_URL || '' %>"></div>
                    </div>
                    <div class="field"><label>User Info URL</label><input type="text" name="OIDC_USERINFO_URL" value="<%= config.OIDC_USERINFO_URL || '' %>"></div>
                </div>
                <button class="nf-btn" type="submit"><i class="fas fa-save"></i> Save & Apply</button>
            </form>
        </div>
    </main>

    <script>
        function switchTab(id, el) {
            document.querySelectorAll('.panel').forEach(p => p.classList.remove('active'));
            document.querySelectorAll('.nav-item').forEach(n => n.classList.remove('active'));
            document.getElementById(id).classList.add('active');
            if (el) el.classList.add('active');
        }
        function showFn(input, span) {
            if (input.files && input.files[0]) document.getElementById(span).innerText = input.files[0].name;
        }
        function editUser(user) {
            window.scrollTo({ top: 0, behavior: 'smooth' });
            switchTab('users', document.querySelectorAll('.nav-item')[1]);
            const form = document.getElementById('userForm');
            form.action = '/admin/user/edit';
            document.getElementById('editUserId').value = user.id;
            document.getElementById('editUsername').value = user.username;
            document.getElementById('editRole').value = user.role;
            document.getElementById('userFormTitle').innerText = "Edit User: " + user.username;
            document.getElementById('editPassword').placeholder = "Leave empty to keep current";
            document.getElementById('editPassword').required = false;
            document.getElementById('passHint').innerText = "(optional)";
            document.getElementById('userSubmitBtn').innerHTML = '<i class="fas fa-save"></i> Update User';
            document.getElementById('userCancelBtn').style.display = 'inline-flex';
        }
        function resetUserForm() {
            const form = document.getElementById('userForm');
            form.action = '/admin/user/add';
            form.reset();
            document.getElementById('editUserId').value = '';
            document.getElementById('userFormTitle').innerText = "Create New User";
            document.getElementById('editPassword').placeholder = "Enter password";
            document.getElementById('editPassword').required = true;
            document.getElementById('passHint').innerText = "";
            document.getElementById('userSubmitBtn').innerHTML = '<i class="fas fa-plus"></i> Create User';
            document.getElementById('userCancelBtn').style.display = 'none';
        }
    </script>
</body>
</html>
