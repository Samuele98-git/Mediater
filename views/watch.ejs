<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Watching: <%= media.title %></title>
    <style>
        body { margin: 0; background: black; overflow: hidden; height: 100vh; display: flex; align-items: center; justify-content: center; }
        video { width: 100%; height: 100%; object-fit: contain; }
        .back-ui { position: absolute; top: 30px; left: 30px; z-index: 100; opacity: 0; transition: opacity 0.3s; }
        body:hover .back-ui { opacity: 1; }
        .btn-back { color: white; text-decoration: none; font-family: sans-serif; font-size: 1.5rem; text-shadow: 0 2px 5px black; display: flex; align-items: center; gap: 10px; }
        .btn-back:hover { color: #E50914; }
    </style>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <div class="back-ui">
        <a href="/" class="btn-back"><i class="fas fa-arrow-left"></i> Back to Library</a>
    </div>

    <video controls autoplay>
        <source src="<%= media.path %>" type="video/mp4">
        Your browser does not support the video tag.
    </video>
</body>
</html>